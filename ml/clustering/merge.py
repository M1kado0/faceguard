"""Offline cluster refinement — DBSCAN/HDBSCAN over the full embedding space."""


async def refine_clusters() -> int:
    """Run a batch consolidation pass. Returns count of merged clusters."""
    raise NotImplementedError
