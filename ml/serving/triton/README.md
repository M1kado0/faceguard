# Triton model repository

Per-model directories with `config.pbtxt` and `1/model.onnx` (or `model.plan` for TensorRT).
Dynamic batching enabled (1-32, max latency 50ms). Models hot-reload on file change.
